/* eslint-disable import/prefer-default-export, no-unused-vars */

/**
 *
 * Provides the different levels of logging
 * @export
 * @enum {number}
 */
export enum LEVELS {
    DEBUG,
    LOG,
    INFO,
    WARN,
    ERROR
}
